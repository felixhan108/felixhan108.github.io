const VersionBadge = ({ version }) => (
  <span className="version-badge">v{version}</span>
)

// 스타일 예시
const styles = `
.version-badge {
  background: #f0f0f0;
  padding: 4px 8px;
  border-radius: 4px;
  font-size: 0.8em;
  font-weight: bold;
}
`
